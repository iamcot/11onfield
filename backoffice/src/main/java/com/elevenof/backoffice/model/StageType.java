package com.elevenof.backoffice.model;

public enum StageType {
    REGIONAL_AUDITION,      // Regional audition stage (public scoring)
    TRAINING_EPISODE,       // Training episode (internal scoring)
    FINAL_MATCH             // Final match (public scoring)
}
