spring:
  datasource:
    url: jdbc:mysql://localhost:3306/elevenof_db_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    com.elevenof.backoffice: DEBUG
